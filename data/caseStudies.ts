import { CaseStudy } from '@/types';

export const caseStudiesData: CaseStudy[] = [
  {
    id: 'cs-1',
    moduleId: 'cloud-fundamentals',
    title: 'Design a Global E-Commerce Platform',
    problemStatement: 'Design a highly available, globally distributed e-commerce platform that handles 10M+ daily active users, supports flash sales with 100x traffic spikes, and maintains sub-200ms page load times worldwide.',
    architectureExplanation: 'Multi-region active-active architecture with CDN edge caching, regional application clusters behind global load balancers, event-driven microservices for order processing, and CQRS pattern for read-heavy product catalogs. Data layer uses regional read replicas with a primary write region and async replication.',
    componentBreakdown: [
      'Global DNS (Route 53 / CloudFront) for geo-routing',
      'Regional Kubernetes clusters for application tier',
      'Redis cluster per region for session and product cache',
      'PostgreSQL with read replicas + DynamoDB for cart/orders',
      'Kafka/SQS for async order processing pipeline',
      'S3 + CloudFront for static assets and images',
      'ElasticSearch for product search across regions',
    ],
    tradeOffs: [
      'Eventual consistency in product catalog vs strong consistency for orders',
      'Higher infrastructure cost for multi-region vs single-region latency',
      'Complexity of distributed transactions vs simpler monolithic approach',
      'CDN cache invalidation lag vs real-time price updates',
    ],
    scalabilityStrategy: 'Horizontal auto-scaling per region based on CPU/request metrics. Pre-warming before flash sales using predictive scaling. Database read replicas scale independently. Caching layer absorbs 80%+ read traffic. Queue-based order processing decouples write spikes from downstream services.',
    failureScenarios: [
      'Region outage: DNS failover to nearest healthy region within 60s, orders in-flight replayed from Kafka',
      'Database primary failure: Automated failover to standby, ~30s downtime, read replicas serve reads during failover',
      'Cache failure: Circuit breaker falls back to database, rate limiting prevents cascade',
      'Payment gateway timeout: Retry with exponential backoff, idempotency keys prevent double charges',
    ],
    costConsiderations: [
      'Multi-region doubles infrastructure cost but provides 99.99% availability',
      'Reserved instances for baseline, spot/preemptible for batch processing saves 60%',
      'CDN costs scale with traffic but reduce origin server requirements',
      'Cross-region data transfer is significant—minimize with regional data locality',
    ],
    targetLevel: 'senior',
  },
  {
    id: 'cs-2',
    moduleId: 'kubernetes',
    title: 'Migrate Monolith to Microservices on Kubernetes',
    problemStatement: 'A 500K LOC monolithic Java application serving 5M requests/day needs to be decomposed into microservices on Kubernetes without downtime, while maintaining data consistency and team velocity during migration.',
    architectureExplanation: 'Strangler Fig pattern: gradually extract services from the monolith behind an API gateway. Start with bounded contexts that have clear domain boundaries. Use event sourcing for data synchronization between monolith and new services. Deploy on Kubernetes with Istio service mesh for traffic management, canary deployments, and observability.',
    componentBreakdown: [
      'API Gateway (Kong/Istio) for routing between monolith and microservices',
      'Kubernetes cluster with namespace-per-team isolation',
      'Istio service mesh for mTLS, traffic splitting, observability',
      'Kafka for event-driven communication between services',
      'Database-per-service with Debezium CDC for data sync',
      'Helm charts + ArgoCD for GitOps deployment',
      'Prometheus + Grafana + Jaeger for observability stack',
    ],
    tradeOffs: [
      'Gradual migration preserves velocity but extends timeline to 12-18 months',
      'Database-per-service improves autonomy but complicates cross-service queries',
      'Service mesh adds latency (~2ms) but provides critical observability',
      'Event sourcing adds complexity but enables audit trail and replay',
    ],
    scalabilityStrategy: 'Each microservice scales independently based on its workload. CPU-intensive services use HPA. Queue consumers scale based on lag. Kubernetes cluster autoscaler adds nodes as needed. Service mesh enables canary releases for safe scaling changes.',
    failureScenarios: [
      'Service mesh control plane failure: Data plane continues with last-known config, fallback to direct communication',
      'Kafka broker failure: Multi-broker cluster with replication factor 3, producers retry with idempotency',
      'Database CDC lag: Circuit breaker on read path, eventual consistency SLA documented per service',
      'Cascading failure: Istio circuit breakers + retry budgets prevent one slow service from taking down others',
    ],
    costConsiderations: [
      'Running both monolith and microservices during migration doubles some infrastructure costs',
      'Service mesh (Istio) adds ~15% resource overhead per pod',
      'Operational complexity requires dedicated platform team (2-3 engineers)',
      'Long-term cost reduction through independent scaling outweighs migration cost',
    ],
    targetLevel: 'staff',
  },
  {
    id: 'cs-3',
    moduleId: 'aws',
    title: 'Design a Serverless Data Pipeline at Scale',
    problemStatement: 'Build a real-time data ingestion and analytics pipeline processing 1M+ events/second from IoT devices, with sub-minute analytics availability, 7-year data retention for compliance, and cost under $50K/month.',
    architectureExplanation: 'Lambda-based serverless architecture with Kinesis for real-time ingestion, Lambda for transformation, S3 for storage with lifecycle policies, and Athena for ad-hoc queries. Real-time dashboards powered by Kinesis Analytics feeding into ElastiCache. Batch processing via Glue for historical analysis.',
    componentBreakdown: [
      'Kinesis Data Streams (sharded) for real-time event ingestion',
      'Lambda functions for ETL transformation and enrichment',
      'S3 with partitioned Parquet files for analytical storage',
      'Kinesis Data Analytics for real-time aggregation',
      'ElastiCache Redis for real-time dashboard metrics',
      'Athena + Glue Catalog for ad-hoc SQL queries',
      'S3 Glacier for long-term compliance archival',
      'CloudWatch + SNS for pipeline monitoring and alerting',
    ],
    tradeOffs: [
      'Serverless eliminates server management but limits execution to 15 min per Lambda',
      'Kinesis provides ordering guarantees but costs more than SQS at scale',
      'Parquet on S3 is cheap storage but slower for point queries vs DynamoDB',
      'Athena is cost-effective for ad-hoc but not suitable for low-latency dashboards',
    ],
    scalabilityStrategy: 'Kinesis shards scale with throughput. Lambda concurrency auto-scales per shard. S3 is effectively unlimited. Partition data by date/device for query performance. Use Kinesis enhanced fan-out for multiple consumers without contention.',
    failureScenarios: [
      'Lambda throttling: Increase reserved concurrency, implement DLQ for failed events',
      'Kinesis hot shard: Re-partition using better partition keys, use random suffix strategy',
      'S3 eventual consistency: Use S3 strong consistency (now default) for read-after-write',
      'Data corruption: Schema validation at ingestion, dead letter queue for malformed events, replay from Kinesis retention',
    ],
    costConsiderations: [
      'Kinesis: $0.015/shard-hour, ~$11K/month for 1M events/sec with 100 shards',
      'Lambda: $0.20/1M requests, significant at scale—batch processing reduces invocations',
      'S3 Standard to Glacier lifecycle reduces storage cost by 80% for old data',
      'Athena charges per TB scanned—Parquet + partitioning reduces cost by 90%',
    ],
    targetLevel: 'senior',
  },
  {
    id: 'cs-4',
    moduleId: 'security',
    title: 'Zero Trust Security Architecture for Multi-Cloud',
    problemStatement: 'Design a zero-trust security architecture for an organization running workloads across AWS, Azure, and on-premise data centers with 5000+ employees, strict compliance requirements (SOC2, HIPAA), and need for unified identity management.',
    architectureExplanation: 'Zero trust assumes no implicit trust—verify every request regardless of network location. Centralized identity provider (Okta/Azure AD) with MFA for all access. Service mesh with mTLS for service-to-service communication. Network microsegmentation via cloud-native firewalls. Secrets managed via HashiCorp Vault with dynamic credentials. Continuous monitoring with SIEM integration.',
    componentBreakdown: [
      'Okta/Azure AD as centralized IdP with MFA and conditional access',
      'HashiCorp Vault for secrets management with dynamic database credentials',
      'Service mesh (Istio/Linkerd) for mTLS between all services',
      'Cloud-native firewalls (AWS SG, Azure NSG) for microsegmentation',
      'SIEM (Splunk/Sentinel) for security event aggregation and correlation',
      'Policy-as-Code (OPA/Rego) for consistent access policies across clouds',
      'WAF + DDoS protection at edge (CloudFront Shield / Azure Front Door)',
    ],
    tradeOffs: [
      'Zero trust adds latency to every request via auth checks vs perimeter-only model',
      'Multi-cloud identity federation is complex but prevents vendor lock-in',
      'Dynamic secrets rotation improves security but adds operational complexity',
      'Comprehensive logging generates large volumes but is required for compliance',
    ],
    scalabilityStrategy: 'Distributed policy evaluation at edge (no central bottleneck). Identity provider handles 10K+ auth requests/second. Vault auto-unseals and clusters across regions. SIEM handles 50K+ events/second with horizontal scaling.',
    failureScenarios: [
      'IdP outage: Cached tokens valid for short period, break-glass emergency access procedures',
      'Vault seal event: Auto-unseal via cloud KMS, failover to standby cluster',
      'SIEM ingestion lag: Buffer in Kafka, alert on lag exceeding SLA',
      'Certificate expiry: Automated rotation via cert-manager, monitoring on expiry dates',
    ],
    costConsiderations: [
      'Okta/Azure AD licensing: $6-12/user/month for premium features',
      'Vault Enterprise: ~$50K/year but saves on security incident costs',
      'SIEM costs scale with log volume—implement log filtering and tiering',
      'Security tooling typically 5-10% of infrastructure budget but prevents 7-figure breaches',
    ],
    targetLevel: 'principal',
  },
  {
    id: 'cs-5',
    moduleId: 'docker',
    title: 'Container Platform for CI/CD at Scale',
    problemStatement: 'Design a container-based CI/CD platform supporting 200+ developers, 500+ daily builds, with build isolation, artifact management, vulnerability scanning, and deployment to multiple environments.',
    architectureExplanation: 'Kubernetes-based build platform using Tekton for CI pipelines. Each build runs in isolated pods with resource limits. Container images built with Kaniko (rootless). Images stored in Harbor registry with automatic vulnerability scanning. GitOps deployment via ArgoCD to dev/staging/prod clusters.',
    componentBreakdown: [
      'Tekton Pipelines for CI execution on Kubernetes',
      'Kaniko for rootless, daemon-less container image builds',
      'Harbor registry for image storage, scanning, and signing',
      'ArgoCD for GitOps-based deployments across environments',
      'Vault for injecting secrets into build pipelines',
      'MinIO/S3 for build artifact and cache storage',
      'Prometheus + Loki for pipeline observability',
    ],
    tradeOffs: [
      'Kubernetes-native CI is powerful but has steeper learning curve than Jenkins',
      'Rootless builds (Kaniko) are more secure but slower than Docker-in-Docker',
      'GitOps simplifies deployment but requires strict Git branch management',
      'Self-hosted registry gives control but requires maintenance vs managed options',
    ],
    scalabilityStrategy: 'Build pods auto-scale on Kubernetes nodes with cluster autoscaler. Build cache shared via PVC speeds repeat builds by 60%. Pipeline parallelism limited by cluster capacity. Harbor replicated across regions for multi-cluster deployments.',
    failureScenarios: [
      'Build node failure: Kubernetes reschedules pod to healthy node, build retries from cache',
      'Registry unavailable: Builds queue, deployments use last-known-good image',
      'ArgoCD sync failure: Manual rollback via Git revert, health checks prevent bad deploys',
      'Secret leak in build: Vault short-lived tokens expire, image scanning catches embedded secrets',
    ],
    costConsiderations: [
      'Dedicated build nodes with spot instances reduce cost by 70%',
      'Build cache reduces compute time and cost by 40-60%',
      'Harbor storage scales linearly—implement image retention policies',
      'Open-source stack (Tekton + ArgoCD + Harbor) eliminates license costs',
    ],
    targetLevel: 'staff',
  },
  {
    id: 'cs-6',
    moduleId: 'terraform',
    title: 'Multi-Account AWS Landing Zone with Terraform',
    problemStatement: 'Design and implement a multi-account AWS landing zone using Terraform for an enterprise with 50+ AWS accounts, centralized networking, security guardrails, and self-service account provisioning for development teams.',
    architectureExplanation: 'AWS Organizations with SCPs for account-level guardrails. Hub-and-spoke networking with Transit Gateway. Centralized logging to security account. Terraform modules for account vending machine pattern. State managed in S3 with DynamoDB locking per account.',
    componentBreakdown: [
      'AWS Organizations with OUs for prod, dev, staging, security, shared-services',
      'Transit Gateway for hub-and-spoke VPC connectivity',
      'Centralized CloudTrail + Config in security account',
      'Terraform Cloud/Enterprise for state management and policy enforcement',
      'Account vending machine: Terraform module to provision new accounts',
      'SCPs for preventive guardrails (e.g., deny region, require encryption)',
      'SSO via AWS IAM Identity Center for centralized access',
    ],
    tradeOffs: [
      'Multi-account isolation improves security but increases networking complexity',
      'SCPs prevent misuse but can block legitimate operations if too restrictive',
      'Centralized Terraform state simplifies management but creates single point of failure',
      'Transit Gateway simplifies routing but adds per-GB data transfer cost',
    ],
    scalabilityStrategy: 'Account vending machine enables self-service provisioning in minutes. Terraform modules versioned and shared via private registry. Transit Gateway scales to 5000+ VPCs. Centralized logging scales with S3 and Athena for querying.',
    failureScenarios: [
      'Transit Gateway failure: Regional redundancy, fallback to VPC peering for critical paths',
      'Terraform state corruption: S3 versioning enables state recovery, DynamoDB prevents concurrent writes',
      'SCP misconfiguration: Staged rollout to test OU first, monitoring for access denied spikes',
      'Account vending failure: Manual account creation as fallback, automated remediation pipeline',
    ],
    costConsiderations: [
      'Transit Gateway: $0.05/GB processed—significant for high-traffic cross-account communication',
      'Multi-account reduces blast radius but increases AWS support plan costs',
      'Centralized logging S3 costs managed with lifecycle policies and Intelligent-Tiering',
      'Terraform Cloud Business: ~$70/user/month but provides policy enforcement and audit',
    ],
    targetLevel: 'principal',
  },
];
